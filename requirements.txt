# DIX VISION v42.2 — BASE runtime dependencies.
#
# ─────────────────────────────────────────────────────────────────[...]
# AUDIT-P0.1 — ML/research dependency extraction
# ─────────────────────────────────────────────────────────────────[...]
# Heavy ML packages (torch, jax, nengo, tianshou, causalml, …) have
# been removed from this file.  They were causing ~5 GB installs and
# 30-minute first-run times on Windows.
#
# Install ONLY what the base operator harness (ui.server) needs at
# import time — FastAPI, pydantic, websockets, the cognitive layer.
# Everything else is opt-in through pyproject.toml extras:
#
#   pip install -e ".[neuromorphic]"   torch / snntorch / bindsnet / brian2
#   pip install -e ".[ml]"             jax / jaxlib / nengo / tianshou
#   pip install -e ".[causal]"         causalml / dowhy / econml
#   pip install -e ".[memory_tensor]"  qdrant-client
#   pip install -e ".[parquet]"        pyarrow
#   pip install -e ".[observability]"  hydra-core / jaeger-client
#   pip install -e ".[evm]"            eth-account (UniswapX adapter)
#
# Base install target: ~100 MB, <2 minutes on a fresh Windows machine.
# ──────────────────────────────────────────────────────────────────[...]
#
# Pins here must stay in sync with pyproject.toml
# [project.dependencies] — bump both in the same commit.

PyYAML>=6.0
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
websockets>=12.0
httpx>=0.25

# wave-03 cognitive layer is imported unconditionally by
# ui.cognitive_chat_runtime (loaded at module-import time by ui.server),
# so these belong in the base runtime, not the cognitive extras.
langchain-core>=0.3,<1
langgraph>=0.2,<1
