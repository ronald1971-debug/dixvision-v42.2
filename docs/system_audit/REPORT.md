# DIX VISION v42.2 — Full System Audit (Phase-6 Regen Report)

_Regenerated by the Phase-6 audit pass against the live filesystem
after PR #443 merged. Drops the stale 719-file picture from PR #183
and aligns `docs/system_audit/` with the canonical
`tools/total_validation.py` 12-phase output set under `analysis/`._

---

## Coverage guarantee (STOP condition)

> **You are NOT finished until: analyzed == total files AND no
> unknown files remain.**

| Metric | Value |
|---|---|
| Git-tracked files in the system | **1,245** |
| Files in `docs/system_audit/file_index.csv` | 1,245 |
| Files marked `analyzed=yes` in `docs/system_audit/tracking.csv` | **1,245 / 1,245 (100 %)** |
| Files with any tooling finding | 631 (51 %, dominated by `ruff format` drift — see Phase 3) |
| Orphan modules (no inbound import, no CI/scripts caller) | **0** |
| Apparent orphan modules (no Python import, but invoked from CI) | **2** (`tools/build_status_generator.py`, `tools/enforce.py`) |
| Orphan registry YAML (no Python reference) | **0 / 18 .yaml** |
| Unmapped tooling rows | 0 |

**STOP condition met.** No unknown files remain.

The canonical `tools/total_validation.py` walks the filesystem
(not just `git ls-files`) and therefore reports a different file
total — 1,794 — because it also covers generated CI fixtures,
`__pycache__`, `analysis/*.csv` snapshots, etc. The audit here
stays on the git-tracked 1,245 set for parity with PR #183's
framing and `git ls-files`.

The two apparent orphans (`tools/build_status_generator.py`,
`tools/enforce.py`) are both **CI entry-points** — `tools/enforce.py`
is invoked from `.github/workflows/total_validation.yml`, and
`tools/build_status_generator.py` from `scripts/build_status.bat`.
The static orphan scan here cannot see those external callers, but
R-5 (PR #445) teaches the canonical `tools/total_validation.py`
phase-7 file-usage detector to count `.github/workflows/*.yml` and
`scripts/*.{bat,ps1,sh}` as valid callers. After PR #445 lands the
canonical detector reports 0 false-positive orphans.

---

## Phase 1–2: Enumeration + Tracking Table

* `docs/system_audit/file_index.csv` — `[file_id, path, size_bytes,
  lang, sha256]` for every git-tracked file (1,245 rows).
* `docs/system_audit/tracking.csv` — `[file_id, path, lang, bucket,
  status, analyzed, issues_found]` joined with bulk-scan findings.
  Every row has `status=analyzed` and `analyzed=yes`.

## Phase 3: Bulk Static Scan

| Tool | Files | Findings | Verdict |
|---|---|---|---|
| `ruff check` | 905 .py | **0** | clean |
| `ruff format --check` | 905 .py | **631** | cosmetic drift — closed by PR #444 (Phase-6 P2-5) |
| `vulture --min-confidence 100` | 905 .py | 24 | small set, mostly Pydantic / LangChain protocol shapes — none changed behaviour at runtime |
| `tools/authority_lint.py` (B1, B7..B36) | 905 .py | **0** | clean |
| `tools/authority_matrix_lint.py` (INV-60) | repo | **0** (8 actors, 11 conflicts, 3 overrides) | clean |
| `tools/constraint_lint.py` (INV-61) | repo | **0** (30 rules, 8 with `when` predicates) | clean |
| `tools/scvs_lint.py` (INV-57) | repo | **0** (23 sources, 3 `consumes.yaml` files) | clean |
| Orphan-module scan (`docs/system_audit/_tools/orphan_scan.py`) | 905 .py | 2 false-positive orphans | both are CI-invoked entry-points; see header |
| Registry coverage (`docs/system_audit/_tools/registry_coverage.py`) | 18 .yaml | **0 orphans** | clean |

**Bulk verdict:** **zero** authority-lint, authority-matrix-lint,
constraint-lint, and SCVS-lint violations. The 631 ruff-format
findings are mechanical and are closed by PR #444 (`ruff format .`
one-shot). The 24 vulture findings are false positives for the
project's Protocol / Pydantic shape conventions and are tracked
case-by-case if any becomes load-bearing.

## Phase 3b: Per-directory deep-read

| Bucket | Files | With findings | Note |
|---|---:|---:|---|
| `.agents` | 1 | 0 | testing-self-evolution skill |
| `.github` | 5 | 0 | CI workflows (`ci`, `crosshair`, `dashboard2026`, `property_tests`, `rust`, `rust_revival_reminder`, `sandbox`, `total_validation`) |
| `_root` | 5 | 0 | top-level metadata |
| `contracts` | 1 | 0 | `events.proto` (POLICY_STATE now present — Phase-6 P0-2) |
| `core` | 54 | 30 | contracts + coherence + constraint-engine (cycle closed by R-4) |
| `dash_meme` | 43 | 0 | memecoin-focused dashboard surface |
| `dashboard2026` | 192 | 0 | TSX/React operator cockpit |
| `dashboard_backend` | 8 | 4 | now in `packages.find` (Phase-6 P0-1) |
| `docs` | 62 | 0 | system_audit + canonical_rebuild + spec |
| `enforcement` | 1 | 0 | re-export facade |
| `evolution_engine` | 35 | 27 | RL + genetic + patch-pipeline |
| `execution_engine` | 40 | 27 | adapter perimeter + paper broker |
| `governance_engine` | 30 | 22 | mode FSM + decision-signer + signal-trust cap (Phase-6 P1-1) |
| `immutable_core` | 2 | 0 | axiom registry |
| `intelligence_engine` | 105 | 52 | signal pipeline + meta-controller + DSPy (C-10) |
| `learning_engine` | 33 | 24 | closed-learning-loop + Phase-6 P1-3 dormant health |
| `opponent_model` | 2 | 1 | behaviour predictor (OPP-01) |
| `registry` | 18 | 0 | YAML — sensory not yet registered (C-4 / P2-2) |
| `scripts` | 7 | 1 | Windows launcher + credential pre-flight |
| `sensory` | 36 | 26 | now in `packages.find` (Phase-6 P0-1); SCVS registry coverage pending (C-4) |
| `simulation` | 25 | 23 | SIM-07..22 step functions + ParallelRunner |
| `simulation_engine` | 3 | 2 | sim contract surface |
| `state` | 26 | 20 | StateReconstructor + Snapshots |
| `system` | 3 | 2 | clock + risk cache (Rust-portable) |
| `system_engine` | 68 | 35 | authority + credentials + ledger + sensors |
| `tests` | 382 | 299 | unit + property + invariant + R-4/R-5 regression |
| `tools` | 25 | 17 | enforce + total_validation + authority_lint family |
| `ui` | 33 | 19 | server + harness + plugin_routes — split tracked under C-2/P2-4/R-1 |

## Phase 4: Validation (orphans, unused modules, declared-but-unused)

* **Orphan Python modules**: 0 true orphans across 905 modules. The
  static scan flags 2 entry-points; both are invoked from CI.
* **Orphan registry YAMLs**: 0 of 18 YAML files under `registry/`.
* **Dependency-graph cycles** (`analysis/dependency_graph.json` after
  R-4):
  - `ui.harness.boot_manager ↔ ui.harness.runtime_registrar ↔ ui.server`
    — known. Closes as a side-effect of the planned `ui/server.py`
    route-split (C-2 / P2-4 / R-1).
  - **Removed** by PR #446 (R-4): `core.constraint_engine ↔
    core.constraint_engine.compiler`.
* **Topology drift** (`analysis/topology_drift.json`): **0 / 28**.
  Every declared runtime node is either active or on the explicit
  `DECLARED_BUT_DORMANT_ALLOWLIST`. Enforced by phase-12 of
  `total_validation.py` (RT chain PR-RT-5).
* **AST validation**: clean.
* **Cross-tier domain violations**: 0 (authority_lint B-CLOCK,
  B-POLARS, B-TORCH, B1, B7..B36 all green).

## Phase 5: Final Report (system health score)

**Overall: B+** (carry from FULL_SYSTEM_ANALYSIS.md).

| Pillar | Score | Note |
|---|---|---|
| Governance / safety | A | hash-chained ledger, signed decisions, kill-switch, decision-trace |
| Validation / regression | A | 12-phase total_validation, authority_lint family, property tests |
| Architecture isolation | A− | tier-violation count 0; one residual `ui.server` cycle |
| Active autonomy | B− | learning + evolution loops wired (Phase-5/P0-A); freeze policy gated |
| Runtime topology authority | A | RT chain (PR-RT-1..5) declares + verifies active topology |
| Sensory perimeter | B− | source contracts complete; SCVS registry coverage pending (C-4) |
| Documentation freshness | B+ | this report regenerates the 719→1,245 delta (was stale) |
| Code-quality drift | C+ | 631 ruff-format findings (PR #444 closes); 24 vulture FPs |
| LIVE-readiness | B− | UniswapX env-var credential migration pending (C-1) |

### Critical issues (carry from FULL_SYSTEM_ANALYSIS.md §5.2)

| Rank | Issue | Status |
|---|---|---|
| C-1 | UniswapX private key in `os.environ` (LIVE-unsafe) | queued — Phase-6 P1-4 |
| C-2 | `ui/server.py` is 3,959 lines / 50+ inline routes | queued — Phase-6 P2-4 / R-1 |
| C-3 | docs/system_audit stale (719 vs 1,245) | **this PR** |
| C-4 | sensory not registered in SCVS | queued — Phase-6 P2-2 |
| C-5 | Learning/Evolution engines reported OK while dormant | **closed by PR #443** |
| C-6 | Per-source confidence cap not on engine SIGNAL_AUDIT | **closed by PR #441** |
| C-7 | hydra-core + jaeger-client not in extras | **closed by PR #442** |
| C-8 | sensory* + dashboard_backend* missing from packages.find | **closed by PR #440** |
| C-9 | POLICY_STATE missing from `contracts/events.proto` | **closed by PR #440** |
| C-10 | DSPy optimizer never invoked on hot path | queued — Phase-6 R-7 |
| C-11 | `ui.server` ↔ `boot_manager` ↔ `runtime_registrar` cycle | closes with C-2 / P2-4 |
| C-12 | 24 INV-XX invariants lack property tests | queued — Phase-6 R-2 |

### Structural weaknesses (carry §5.3)

1. **`ui/server.py` god-object** — biggest maintainability hazard. R-1 split planned.
2. **Documentation drift** — closed by this regen. CI now keeps `tools/total_validation.py`
   running on every push; `docs/system_audit/_tools/` provides the git-tracked-only view.
3. **Spec-vs-implementation drift** — 58 IDs in limbo under R-3.
4. **DSPy optimizer dangling** — R-7 decision pending.
5. **TSX header convention** — 184 files; R-6.

### Refactor priorities (carry §5.4)

| # | Priority | Status |
|---|---|---|
| R-1 | Split `ui/server.py` into `ui/routes/*` | queued |
| R-2 | Property-test pins for 24 UNVERIFIED INV-XX | queued |
| R-3 | Spec hygiene (`declaration_map.csv` MISSING rows) | queued |
| R-4 | Break `core.constraint_engine ↔ .compiler` cycle | **closed by PR #446** |
| R-5 | Dead-file detector counts CI / script callers | **closed by PR #445** |
| R-6 | TSX header convention | not started |
| R-7 | DSPy optimizer — wire or extract to `tools/` | not started |
| R-8 | `ruff format .` one-shot | **closed by PR #444** |

---

## Regen workflow

This report is produced by the `docs/system_audit/_tools/` script
chain — the same chain that produced PR #183 — and the canonical
`tools/total_validation.py` 12-phase output set under `analysis/`.
To regenerate locally:

```bash
python docs/system_audit/_tools/enumerate.py
python docs/system_audit/_tools/build_tracking.py
python docs/system_audit/_tools/bulk_scan.py
python docs/system_audit/_tools/finalize_tracking.py
python docs/system_audit/_tools/orphan_scan.py
python docs/system_audit/_tools/registry_coverage.py
python docs/system_audit/_tools/build_plan_stage.py
python tools/total_validation.py --advisory   # writes analysis/*
```

The artefacts under `docs/system_audit/` (this directory) cover the
git-tracked perimeter for human-readable review. The artefacts under
`analysis/` cover the filesystem perimeter for CI gating (`tools/enforce.py`).
