"""Minimal UI harness for the engine bus (Phase E1).

This is **not** the full cockpit (build_plan.md §Phase E7). It is a small
FastAPI + vanilla-JS surface that lets an operator drive the engines
end-to-end while real plugins / dashboards are still unbuilt.
"""
